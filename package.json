{
  "name": "ai-tinder-backend",
  "version": "1.0.0",
  "description": "Backend API for AI Tinder clone — tracks like/nope/superlike decisions",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --testEnvironment node tests/server.test.js && jest --testEnvironment jsdom tests/app.test.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jest-environment-jsdom": "^30.3.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "supertest": "^7.0.0"
  }
}
